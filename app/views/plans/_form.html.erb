<%= f.text_field :advertiser_name %>

<% f.fields_for(:buys) do |b| %>
  <fieldset>
    <legend>
      <% if b.object.new_record? %>
      Create Buy
      <% else %>
      Update Buy
      <% end %>
    </legend>
    <%= render :partial => "buys/form", :locals => { :f => b } %>
  </fieldset>
<% end %>